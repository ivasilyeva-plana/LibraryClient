export class Writer {
    public id: string;
    public name: string;
    public country: string;

    constructor(id, name, country) {
        this.id = id;
        this.name = name;
        this.country = country;
    }
}